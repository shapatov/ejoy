#include "grader67.h"
#include "grader67.cpp"
#include <bits/stdc++.h>
using namespace std;
vector<int> solve(int n) {
    vector<int> ans(n);
    for(int i = 0; i < n; i++) cin >> ans[i];
    return ans;
}